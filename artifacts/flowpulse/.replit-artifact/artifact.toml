kind = "web"
previewPath = "/"
title = "FlowPulse"
version = "1.0.0"
id = "artifacts/flowpulse"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24111

[services.development]
run = "pnpm --filter @workspace/flowpulse run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/flowpulse", "run", "build" ]
publicDir = "artifacts/flowpulse/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24111"
BASE_PATH = "/"
